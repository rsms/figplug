// Note: this is used as both a template for new projects as well
// as the default config file for projects that does not declare one.
// Therefore this needs to be fully functional.
{
  "compilerOptions": {
    "target": "es2017",
    "lib": [
      "es2017",
      "dom"
    ]
  },
  "include": []
}
